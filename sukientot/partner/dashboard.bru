meta {
  name: dashboard
  type: http
  seq: 1
}

get {
  url: {{local_url}}partner/dashboard
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
