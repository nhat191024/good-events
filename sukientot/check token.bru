meta {
  name: check token
  type: http
  seq: 4
}

get {
  url: {{local_url}}check-token
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
